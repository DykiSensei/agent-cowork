# 内部指南（CLAUDE.md / AGENTS.md）待更新的内容 —— M10

这两份在 `7c26bfe` 里移出了仓库，我改不到你手上那份。下面是**这几轮里它们过期的
全部地方**，按「找到什么、换成什么」列出，改完可以删掉这个文件。

事实核对时间：M10 完成、`tools-and-ux` 分支。测试 471 个（含 5 个 skip）。

---

## 1. 测试数与自动化门（两份都有）

**找**：`404 个测试` / `423 个测试` / `455 个测试`
**换**：`471 个测试`

**找**：任何写着「自动化门只有两道」的句子
**换**：

```
没有 linter / formatter。全部自动化门只有三道：
  python -m unittest discover -s tests -t .    471 个测试
  cd ui && npx tsc --noEmit                    形状
  cd ui && npm run check                       行为（翻译层用例表 + 渲染冒烟）
后两道都串在 npm run build 里。别去找 ruff / black，没有。
```

---

## 2. 环境变量表（CLAUDE.md 有一张）

现在代码里真实存在的是这 17 个，**新增了 6 个**（标 ★）：

| 变量 | 作用 |
|---|---|
| `COWORK_ENV_FILE` | 指定 .env 位置 |
| `COWORK_LLM_BASE_URL` | 端点覆盖（**注意**：`probe_provider` 现在也认它了） |
| `COWORK_LLM_API_KEY` | key 覆盖 |
| `COWORK_PG_DSN` | Postgres |
| `COWORK_ARCHITECT_MODEL` / `COWORK_SUBAGENT_MODEL` / `COWORK_TRIAGE_MODEL` | 模型 id 覆盖 |
| `COWORK_ARCHITECT_EFFORT` / `COWORK_SUBAGENT_EFFORT` / `COWORK_CHEAP_EFFORT` | 推理挡位 |
| `COWORK_REVIEW_WRITES` | 写入侧复核开关（on/off，默认 on） |
| ★ `COWORK_ARCHITECT_PROVIDER` | 架构师用哪一家（空 = 自动） |
| ★ `COWORK_REVIEWER_PROVIDER` | 复核者用哪一家（`none` = 关掉独立复核） |
| ★ `COWORK_SUBAGENT_PROVIDER` | Subagent 用哪一家（空 = 跟架构师同一家） |
| ★ `COWORK_WORKSPACE` | 默认工作区（空 = `~/cowork-workspaces`） |
| ★ `COWORK_ALLOWED_BINARIES` | `run` 能调哪些程序，逗号分隔（默认 `python`） |
| ★ `COWORK_ALLOW_NETWORK` | `fetch_url` 开关（on/off，**默认 off**） |

**providers 和 models 是两件事**：后者是模型 id，前者是「谁来干」。

---

## 3. 代码地图（两份都有）

**加三行**：

```
workspace.py    工作区：路径校验 + 产物落点 + 接手时的现状快照。确定性，无 LLM
ui/check/       前端的两道行为检查（translate.mjs 用例表 / render.mjs 渲染冒烟）
ui/src/components/  Progress（现在在干什么）/ Details（技术细节抽屉）/
                    FolderPicker（服务端列目录、界面上点）/ NewTask / Settings
```

**删一行**：`ui/src/components/pro/` —— 专业版已弃用（它独有的信息搬进
`Details.tsx` 的折叠抽屉，没有跟着删）。

`runtime/sandbox.py` 那一行补一句：**八个工具**（write/read/list/search/delete/
move/run/fetch_url），`spec.tools` 是执行白名单。

---

## 4. 「踩过的坑」要新增的条目

按代价从大到小：

- **工具面的缺口不会表现成「做不到」**，会表现成「模型绕路 → 撞白名单 →
  假信号 → 白烧一轮架构师」（§11.6f：缺列目录，75 次运行 23 次假
  SCOPE_VIOLATION）。M10 按这条补了 search/delete/move/recursive。
- **`run` 在本地沙箱里不受 scope 约束。** sandbox.py 里那句「即使 run 绕过
  工具层，内核层面也写不动」**只在 `use_docker=True` 时成立**。所以缺一个受约束
  的 `delete_file`，实际效果是把删除推到唯一一条不受约束的路上。
- **同样叫「声明」，方向可以相反**：`hard_signals` 是「预期」，不该当过滤器
  （漏报一条真实失败比多报一条超出预期的贵）；`spec.tools` 是「许可」，必须当
  过滤器。两条都有用例钉着，别改错方向。
- **白名单不告诉模型就是陷阱**：模型按 system 里的全集去调，撞任务级白名单变成
  SCOPE_VIOLATION —— 正是我们要避免的那种假信号。所以 Subagent 上下文里有
  「这个任务可用的工具」和「run 允许的可执行文件」。
- **探测函数最重要的是不误报。** `HTTPError` 是 `URLError` 的子类，于是
  「这家没有 /v1/models」（404）和「限流」（429，**说明 key 是对的**）全被归成
  unreachable → 用户去改一个本来就对的配置。现在按状态码分开。
- **HTTP 头是 latin-1**：非 ASCII 域名/路径不先编码就发，得到的是
  `UnicodeEncodeError: 'latin-1' codec` —— 一个模型看不懂的错误，它会以为是
  网站的问题。`fetch_url` 现在做 IDNA + percent 编码。
- **广播不是记录。** `architect.plan()` 的日志原来走 SSE 广播：没人订阅就消失、
  刷新也拿不回来 —— 于是「架构师在干什么」在界面上是一段空白。要落成事件。
- **「不在跑」不等于「已经结束」。** PENDING / 拆解中 / 刚派发都不是终局，
  把它们并进 else 分支就会对用户说「这条任务已经结束了」。

---

## 5. 头部状态段

M0–M8 那句改成 **M0–M10**，并补一句：

> M9 会话级 token 硬护栏；M10 工作区与接手已有项目、按角色选供应商、
> 工具面 4→8。**M2/M7 的基线数据在 M10 之后失效**（动了 `ACTION_SCHEMA` 和
> `SUBAGENT_SYSTEM`），新基线在 `bench_runs_m10.jsonl`，旧的 `bench_runs.jsonl`
> 保留不动。

---

## 6. 命令表补两条

```bash
cd ui && npm run check                  # 前端两道行为检查
python -m cowork.cli bench --out bench_runs_m10.jsonl   # 注意：run_batch 用 "w"
                                        # 打开，**默认会覆盖 bench_runs.jsonl**
```

最后那条是新踩的坑：跑批默认写 `bench_runs.jsonl`，而那是 M2 的 1.62M token
原始证据、`policy.py` 每个参数都引它。重测一定要换 `--out`。
